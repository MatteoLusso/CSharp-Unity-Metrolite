public enum CardinalPoint{
    North,
    South,
    East,
    West,
    Random,
    None,
}

public enum Side{ 
    Left,
    Right,
    Top,
    Bottom,
}

public enum Orientation {
    Horizontal,
    Vertical,
}

public enum Direction
{
    None,
    Forward,
    Backward,
}

